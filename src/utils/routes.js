const BASE_ROUTE = `https://cashingames-mock.heroku.app`
const USER_ROUTE = `${BASE_ROUTE}/user`

const API_MAP = {
    GET_USER: `${USER_ROUTE}`,
    GET_LEADERBOARD: `${USER_ROUTE}/leaderboard`,
}

export default API_MAP
